<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Informa que o aplicativo pode utilizar a biblioteca de apps para carros. -->
    <uses-feature
        android:name="android.software.car.app.library"
        android:required="false" />

    <!-- Permissão necessária para templates de mídia no Android Auto. -->
    <uses-permission
        android:name="androidx.car.app.MEDIA_TEMPLATES" />

    <application
        android:name="${applicationName}"
        android:label="Billhard"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Configuração utilizada pelo Flutter. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Arquivo que descreve a compatibilidade com Android Auto. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Nível mínimo da API do Android for Cars App Library. -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <!-- Serviço responsável por exibir a aplicação no Android Auto. -->
        <service
            android:name="com.oguzhnatly.flutter_android_auto.AndroidAutoService"
            android:exported="true">

            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.MEDIA" />
            </intent-filter>

        </service>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>